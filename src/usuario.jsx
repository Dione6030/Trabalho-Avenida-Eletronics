// Arquivo descontinuado: a página de perfil foi substituída por um modal (SweetAlert2) acionado no Header.
// Mantido vazio apenas para compatibilidade caso algum import legado ainda exista.
export default function Usuario() {
  return null;
}
